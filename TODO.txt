- Refactor cookie client ID code into a Client type.
- Client to tell the server which group it belongs to.
- Prove websockets connectivity.

DONE:

- Give clients an identifier (probably via a cookie).

